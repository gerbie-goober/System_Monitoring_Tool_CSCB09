#include <stdio.h>

#ifndef _USER_FUNCTIONS_H
#define _USER_FUNCTIONS_H

/*
  loops through utent file to find users connected to given system and prints user info (name, ut_line, host)
*/
void write_user_usage();

#endif